/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // assetPrefix: './' <--- DELETE THIS LINE IF IT IS THERE
};
export default nextConfig;